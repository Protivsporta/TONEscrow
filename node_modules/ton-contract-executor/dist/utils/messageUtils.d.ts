import { RawCurrencyCollection, RawMessage, Slice } from "ton";
export declare function readCurrencyCollection(slice: Slice): RawCurrencyCollection;
export declare function readMessage(slice: Slice): RawMessage;
